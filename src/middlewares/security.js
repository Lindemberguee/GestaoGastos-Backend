// src/middlewares/security.js

// Security middleware has been removed per your request.
// If you need to reintroduce protections (CORS, rate-limiting, sanitization, etc.),
// consider using Zod in controllers and dedicated middleware packages.

export default function applySecurity(app) {
  // Nenhuma camada de segurança aplicada
}
